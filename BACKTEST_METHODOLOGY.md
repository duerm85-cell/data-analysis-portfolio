# 回测方法说明

## 当前策略逻辑

按 T 日收盘时已经可获得的预测信号排序，选取预测分数最高的 Top-N 股票，等权持有并按日换仓。策略不额外加入止盈止损规则。

## 交易时间轴

```text
T 日收盘后形成信号
        ↓
T+1 日开盘按目标组合调仓（加入买入/卖出滑点）
        ↓
持仓至少跨过 T+1 日，下一交易日开盘才允许卖出或再平衡
```

回测脚本从行情表按 `code + date` 对齐下一交易日 `open/close`。缺少下一日价格的记录不参与真实执行收益计算。没有涨跌停或停牌状态字段时，代码只能检查价格/成交量是否可用，不能声称完整模拟无法成交。

## 收益计算方式

组合收益按相邻交易日开盘估值的净值变化计算：

```text
下一次开盘持仓市值 / 上一次开盘后组合净值 - 1
```

调仓时买卖价格分别加入滑点，组合收益扣除按成交金额计算的佣金和印花税。缺少行情价格的股票不会被凭空补价；真实流程不会使用 `actual` 标签替代执行价格。

## 成本模型

- 佣金：买卖双边均按 0.03% 计入；
- 印花税：卖出单边按 0.05% 计入；
- 滑点：买入和卖出各按 0.10% 的价格偏移计入；
- 换手：按实际成交金额相对调仓前净值计算。

## 研究限制

当前行情缺少逐日涨跌停状态、停牌状态和订单成交量约束，因此这些因素没有被完整模拟；价格缺失会被排除。基准使用可获得的指数开盘估值，与策略的开盘调仓和跨日持仓口径保持一致，但仍不代表完整的指数交易成本。结果只代表当前数据、股票池、参数和时间区间，不代表实盘可得收益。

## 最新 Phase 3 运行记录

使用剔除跨区间标签边界日后重新训练的 XGBoost 分类概率运行 `backtest.py`，`next_open_v2` 生成 229 个开盘估值点：策略总收益 -28.31%，沪深 300 开盘基准 +2.18%，超额 -30.49%，最大回撤 -37.32%，夏普 -0.88，平均换手 77.95%。成交日志记录 4,042 笔成交及 1 笔缺少开盘价的阻塞。结果表明当前信号不具备实盘优势。
