# 最终项目状态说明

## 1. 项目定位

本项目是量化数据工程与研究分析平台，也是面向校招和实习面试的作品集。它要证明作者能够处理数据来源、分层加工、质量校验、数据库服务、时间序列实验、回测执行和结果解释。它不以收益率或高 AUC 证明价值。

## 2. 项目架构

项目有两条相互区分的运行口径：

1. 本地真实研究链路：真实历史行情 → Raw → Clean → 质量检查 → 因子表 → XGBoost/BiLSTM → XGBoost 样本外概率 → `next_open_v2` 回测。
2. 公开展示链路：确定性合成数据包 → 只读 SQLite 服务库 → Streamlit；模型和回测页面读取本地真实研究生成的脱敏摘要。

PySpark 是从 Pandas 流程向批处理迁移的可选实现，不是线上服务依赖。

## 3. 数据流

- Raw / ODS 保存贴源行情和沪深 300 基准，便于追溯原始字段。
- Clean / DWD 统一日期、代码和数值类型，处理重复、非法价格和 OHLC 问题。
- Processed / DWS 计算收益、均线、动量、反转、波动率、布林带和量价特征，并生成下一交易日标签。
- 数据质量模块记录覆盖范围、重复键、缺失、非法值和来源状态。
- 本地 Serving 使用 SQLite 保存完整研究数据；公开 Serving 使用合成数据构建只读 `demo_serving.db`。

质量报告为 WARN 的主要原因是滚动窗口开始阶段的结构性缺失、每只股票最后一日没有下一日标签，以及需要人工复核的极端收益。重复主键、非法日期、非法 OHLC、非正价格、负成交量和非法股票代码当前为 0。

## 4. 模型实验

主要任务统一为“预测下一交易日涨跌”，没有把回归结果和分类指标混在一起。

- 股票池：同一本地 364 只股票研究快照；
- 特征：相同 21 个技术特征；
- 切分：70%/15%/15% 时间顺序切分；
- 边界：剔除 2024-10-10 和 2025-09-15 两个会跨区间引用下一日标签的信号日；
- 测试：2025-09-16 至 2026-08-27，共 83,259 个共同测试目标；
- XGBoost：使用单日技术特征输出上涨概率；
- BiLSTM：使用 20 日序列，scaler 只在训练集拟合，验证集用于早停；
- 计算限制：BiLSTM 从 381,668 个训练序列中确定性选取 120,000 个，验证和测试目标完整评估。

当前结果：

| 模型 | Accuracy | AUC | Precision | Recall | F1 |
|---|---:|---:|---:|---:|---:|
| XGBoost | 52.71% | 0.5276 | 49.06% | 42.43% | 45.50% |
| BiLSTM | 51.43% | 0.5185 | 47.83% | 48.22% | 48.03% |

两个模型都只表现出弱区分能力。XGBoost 的 Accuracy、AUC 和 Precision 略高，BiLSTM 的 Recall 和 F1 略高；这些差异不能证明稳定预测能力。

## 5. 回测实验

回测使用 XGBoost 的样本外上涨概率做横截面排序，每日选择 Top 10 等权持有。执行时间轴是：

```text
T 日收盘形成信号
→ T+1 开盘按目标组合调仓
→ 持有至下一交易日开盘估值或再平衡
```

成本包含买卖双边 0.03% 佣金、卖出 0.05% 印花税、买卖各 0.10% 固定滑点。缺少开盘价时订单阻塞，不使用真实标签或未来价格补造收益。

当前结果：

- 策略收益 -28.31%；
- 沪深 300 开盘基准 +2.18%；
- 超额收益 -30.49%；
- 最大回撤 -37.32%；
- Sharpe -0.88；
- 平均换手率 77.95%；
- 成交 4,042 笔，缺少开盘价阻塞 1 笔。

策略跑输的主要原因是模型信号接近随机水平，而日频 Top-N 调仓产生高换手；佣金、印花税和滑点持续侵蚀净值。这个结论应原样保留。

## 6. 数据库

本地研究库由 `database_manager.py` 管理，使用 SQLite、动态安全建列、`(code, date)` 唯一约束、事务回滚、分块 upsert 和参数化查询。

公开网站读取 `portfolio_data/demo_serving.db`。该库由固定种子的合成数据构建，包含资产维表、300 个合成资产的明细、市场与行业聚合、因子 IC、质量记录和流水线记录。它适合只读作品集服务，不代表生产数据库架构。

## 7. Streamlit

应用保留 10 个现有页面：数据平台、市场总览、股票画像、行业分析、系统概览、数据洞察、因子研究、情绪分析、模型验证和策略回测。

公开模式：

- 查询合成 SQLite 服务库；
- 显示本地研究的脱敏模型和回测摘要；
- 不加载模型权重，不执行在线推理；
- 不访问用户数据库，不需要 Tushare Token；
- 页面明确标记合成演示、非实时和非投资建议。

本地模式可以读取完整研究数据和模型文件，但仍属于研究工具。

## 8. 测试

当前验证覆盖：

- Python 语法检查；
- 全仓库 `compileall`，排除历史 `archive/`；
- 数据质量、数据库、因子、Demo、公开数据包和回测测试；
- Streamlit AppTest 动态检查 10 个页面；
- 公开训练日志、模型对比、特征重要性和回测快照一致性。

Phase 4 实际验证结果为：Python 语法检查通过，`compileall` 通过，26 项测试和 5 项参数化子测试通过，10 个页面通过公开模式烟雾测试。

## 9. 当前结果

对外只使用以下结论：

- XGBoost：Accuracy 52.71%，AUC 0.5276，Precision 49.06%，Recall 42.43%，F1 45.50%；
- BiLSTM：Accuracy 51.43%，AUC 0.5185，Precision 47.83%，Recall 48.22%，F1 48.03%；
- `next_open_v2`：策略 -28.31%，沪深 300 +2.18%，超额 -30.49%，最大回撤 -37.32%，Sharpe -0.88，平均换手 77.95%，成交 4,042 笔，缺少开盘价阻塞 1 笔。

`training_log.json` 和 `backtest_results/` 是本地完整来源；`portfolio_data/` 保存可提交的脱敏镜像。README、Streamlit 和报告必须从这一口径解释。

## 10. 已知限制

- 本地股票池不是完整历史时点股票池，存在幸存者偏差风险；
- 复权口径和跨源价格差异仍需系统核验；
- 停牌、逐日涨跌停、退市和订单成交量约束不完整；
- 固定滑点不能代表真实市场冲击；
- 情绪来源标签尚不支持可信的模型增益结论；
- BiLSTM 训练集采用确定性序列上限；
- 公开 Demo 是合成数据，不能用于市场结论；
- SQLite 与 Streamlit 的容量和并发能力有限。

## 11. 不应该对外声称的能力

- 不声称模型能稳定预测涨跌；
- 不声称策略能够稳定盈利或跑赢沪深 300；
- 不声称项目是 AI 交易机器人；
- 不声称项目是生产级量化交易系统；
- 不声称 PySpark 已在生产集群运行；
- 不声称已有 Kafka、Flink 或 Airflow 生产链路；
- 不声称公开 Demo 的 5,200 个资产是真实股票行情；
- 不把回测结果作为投资建议；
- 不把特征重要性解释为因果关系。

## 12. 面试重点

作者应按下面顺序讲解：

1. 为什么要把真实研究数据与公开合成 Demo 分开；
2. Raw、Clean、Processed、Serving 每层解决什么问题；
3. WARN 中哪些缺失是结构性的，哪些需要继续治理；
4. 下一交易日标签如何生成，为什么需要按时间切分和剔除边界信号日；
5. XGBoost 与 BiLSTM 怎样使用同一测试目标，BiLSTM 为什么有训练序列上限；
6. XGBoost 概率如何进入 Top-N 排序；
7. 为什么 T 日信号必须在 T+1 开盘执行；
8. 佣金、印花税、滑点和高换手怎样影响净值；
9. 为什么 AUC 略高于 0.5 仍没有产生超额收益；
10. SQLite、参数化查询、upsert、缓存和测试怎样支撑 Streamlit；
11. PySpark 当前做到什么，哪些生产组件尚未实现；
12. 下一步应优先补数据边界，而不是继续调参追收益。

如果能不看文档完整讲清这条链路，项目才真正成为作者自己的面试作品。
