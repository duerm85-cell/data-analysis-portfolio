# 模型实验设计

## 实验目的

比较不同模型在同一股票池、同一时间切分和同一输入因子下，对“下一交易日上涨/下跌”的样本外分类能力。目标是识别信号强弱和验证流程是否可信，不追求更高 AUC。

## 主要任务与标签

主要任务统一为二分类：`label = ret.shift(-1) > 0`，即预测下一交易日收益为正还是非正。最后一个交易日因没有下一日收益而不进入训练或测试。历史 XGBoost 回归和线性回归结果保留为辅助诊断记录，不与主要分类实验的准确率/AUC 混合比较。

## 输入特征

使用现有 21 个技术特征：收益、均线、均线差、RSI、MACD、动量、反转、波动率、布林带、成交量和成交额相关指标。来源未验证的情绪特征会被训练流程排除。

## 实验分组

| 组别 | 模型 | 任务 |
|---|---|---|
| Baseline | 0.5 随机方向基线（当前代码未新增 Logistic Regression） | 下一交易日涨跌分类 |
| Tree Model | XGBoost | 下一交易日涨跌分类 |
| Deep Learning | BiLSTM | 下一交易日涨跌分类 |

## 数据与训练方式

数据来自本地研究快照：364 只股票，过滤后特征数据范围为 2020-02-10 至 2026-08-27。两个分类实验均采用 70%/15%/15% 的时间切分，不随机打乱。由于标签引用下一交易日收益，2024-10-10 和 2025-09-15 两个分界信号日被剔除：训练集截至 2024-10-09，验证集为 2024-10-11 至 2025-09-12，测试集为 2025-09-16 至 2026-08-27。BiLSTM 的 scaler 只在训练集拟合，验证集用于早停，完整 83,259 个测试目标只用于最终评估。为保证普通笔记本可复现，BiLSTM 从 381,668 个训练序列中按固定规则等距选取 120,000 个；这一计算约束需要与 XGBoost 全量训练的差异一起披露。公开 Demo 的合成数据只用于页面展示，不替代本地研究结果。

## 评价指标

统一记录 Accuracy、ROC-AUC、Precision、Recall 和 F1；同时记录 MSE 作为概率误差诊断。AUC 接近 0.5 时，说明当前样本中没有明显的排序预测优势，不能解释为可交易优势。

## 结果解释

最新统一测试结果：XGBoost Accuracy 52.71%、AUC 0.5276、F1 0.4550；BiLSTM Accuracy 51.43%、AUC 0.5185、F1 0.4803。两者都只是略高于随机区分水平，结论是技术因子信号较弱。模型页展示离线评估快照和实验边界，不提供稳定盈利或投资建议。

## 特征重要性

最新 XGBoost 分类训练的 Top 10 为：`reversal_5d`、`close_open_ratio`、`ret_5d`、`bb_position`、`momentum_20d`、`high_low_ratio`、`amount_ratio`、`ma10`、`volatility_20d`、`ma20`。其中短期反转、当日开收盘关系和近 5 日收益排在前列，说明模型主要依赖短周期价格变化；布林带、动量、振幅、量额、均线与波动率提供补充。重要性表示树模型内部的相对分裂贡献，不能解释贡献方向、因果关系或未来收益保证。公开模型页读取 `reports/xgb_classifier_feature_importance.csv` 展示 Top 10；SHAP 不作为本阶段依赖。
